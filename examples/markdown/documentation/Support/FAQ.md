# FAQ

## How do I upload these files?

From the CLI repo root, with `APIDEX_HOST` and `APIDEX_TOKEN` set:

```bash
node src/index.js upload-markdown examples/markdown --host "$APIDEX_HOST" --token "$APIDEX_TOKEN"
```
