openapi: "3.2.0"
info:
  title: Echo V1
  summary: Canonical Echo V1 spec for local CLI smoke tests.
  version: "1.1.0"
  description: OpenAPI 3.2 Echo V1 spec for local CLI smoke tests.
paths:
  /pets:
    get:
      summary: List pets
      responses:
        "200":
          description: ok
  /internal:
    get:
      summary: Internal only
      responses:
        "200":
          description: ok
webhooks:
  echoReceived:
    post:
      summary: Fired when an echo is received
      responses:
        "200":
          description: Received
